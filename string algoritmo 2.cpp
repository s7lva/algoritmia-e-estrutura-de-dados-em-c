#include <stdio.h>
#include <stdlib.h>
#include <time.h>


void printStr(char str[]);
void reverseText();
void alphabetChars(char str[]);
void charTypeCount(char str[]);
void wordSort();
int palindroma(char str[]);

int main(){
	char nome[50], text[40];
	
	
	//lê a string
	printf("\nNome Completo: ");
	gets(nome);
	puts(nome);
	
	printStr(nome);
	charTypeCounts(nome);
	alphabetChars(nome);

}


void charTypeCount(char str[]){
	
	int i , vog=0 , cons=0 , digit=0 , space=0 ;
	
	for(i)
	
	
	
}
